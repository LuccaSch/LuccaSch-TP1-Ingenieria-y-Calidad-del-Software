# Configuración de Spring y el server Tomcat
spring.application.name=tp
server.port=4400

# Configuración PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/DSDatabase
spring.datasource.username=admin_ds
spring.datasource.password=root

# Configuración de Hibernate (JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuración de Thymeleaf (para mostrar las vistas en direcciones definidas por nosotros)
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Evita el cacheo durante el desarrollo LUEGO ELIMINAR
spring.thymeleaf.cache=false 
